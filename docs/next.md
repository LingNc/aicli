- 版本号升级的时候应该支持比如1.12 > 1.11 这种形式的可能某个版本配置文件多次变动。
- 配置文件升级的时候是否正确的保留了用户的白名单、禁用列表...request和thinkbody中的内容。我们升级的时候只是对用户没有设置的字段填充默认值。
- 使用less查看log而不是vi
- 为所有用户安装的时候是否写入了比如系统的环境变量，用户登录之后就自带有shell集成无需每个用户子自己再install。还有卸载的时候除了提供用户卸载还有为所有用户卸载的功能吧。
- 对于shell的历史当前剔除了 ai xxxx 只保留了输出的命令，应该保留这个用户的提示。

- 包装screen的界面库，做一个简单的raw的操作方式的，方便其他的进行调用，并且对界面每次光标位置和绘制范围有调试，方便控制。
  - 检查所有输出命令的地方如果命令长度超过当前终端的宽度会导致自动换行，如果这个时候下面不在新的光标下面去输出会导致错误的重叠和覆盖。
  - 应该需要很多地方都对终端窗口进行大小获取，包括提示词中也需要，以便更好的了解终端的变化。
  - 预先设置颜色

- 全平台适配，适配win/mac/linux/android...